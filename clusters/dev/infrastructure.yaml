# clusters/dev/infrastructure.yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: open-data-platform

  # Points to the infrastructure
  path: ./infrastructure/base
  prune: true
  wait: true
  dependsOn:
    - name: crds
