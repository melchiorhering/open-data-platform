apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sail
  namespace: default
spec:
  values:
    # DEV OVERRIDES
    gateway:
      host: "sail.localhost"
    image:
      repository: sail
      tag: latest
      pullPolicy: Never # Critical for local dev
